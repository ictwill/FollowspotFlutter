name: followspot_application_1
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  json_annotation: ^4.8.1
  uuid: ^4.0.0
  pdf: ^3.10.8
  printing: ^5.12.0
  open_file: ^3.2.1

  provider: ^6.0.0
  window_size:
    git:
      url: https://github.com/google/flutter-desktop-embedding.git
      path: plugins/window_size
  window_manager: ^0.3.0
  file_picker: ^5.2.5
  flutter_colorpicker: ^1.0.3
  flutter_form_builder: ^9.1.0
  basic_utils: ^5.5.1
  flutter_svg: ^2.0.2
  shared_preferences: ^2.0.18
  intl: ^0.18.1
  go_router: ^10.1.2
  path_provider: ^2.1.1
  flutter_to_pdf: ^0.1.1
  flutter_iconpicker: ^3.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  
  build_runner: ^2.3.3
  json_serializable: ^6.6.0
  


flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    - assets/images/